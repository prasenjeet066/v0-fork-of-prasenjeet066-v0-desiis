import VerificationBadge from "./badge/verification-badge"

/**
 * Re-export of VerificationBadge so it can be imported from
 * "@/components/verification-badge" as either a default or named export.
 */
export { VerificationBadge }
export default VerificationBadge
